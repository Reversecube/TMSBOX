<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <record id="view_tms_waybill_form" model="ir.ui.view">
            <field name="name">tms.waybill.form</field>
            <field name="model">tms.waybill</field>
            <field name="arch" type="xml">
                <form string="Waybill">
                    <header>
                        <button name="action_confirm" string="Confirm" type="object" class="oe_highlight" invisible="state != 'draft'"/>
                        <button name="action_in_transit" string="Start Transit" type="object" class="oe_highlight" invisible="state != 'confirmed'"/>
                        <button name="action_deliver" string="Deliver" type="object" class="oe_highlight" invisible="state != 'in_transit'"/>
                        <button name="action_cancel" string="Cancel" type="object" invisible="state in ('delivered', 'cancelled')"/>
                        <button name="action_reset_to_draft" string="Reset" type="object" invisible="state != 'cancelled'"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,in_transit,delivered"/>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <h1><field name="name" readonly="1"/></h1>
                        </div>
                        <group>
                            <group>
                                <field name="date"/>
                                <field name="partner_id"/>
                                <field name="driver_id"/>
                                <field name="vehicle_id"/>
                            </group>
                            <group>
                                <field name="route_id"/>
                                <field name="travel_id"/>
                                <field name="origin"/>
                                <field name="destination"/>
                            </group>
                        </group>
                        <group>
                            <group>
                                <field name="cargo_weight"/>
                                <field name="cargo_volume"/>
                            </group>
                            <group>
                                <field name="amount_total" widget="monetary"/>
                                <field name="currency_id" invisible="1"/>
                                <field name="company_id" invisible="1"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Cargo">
                                <field name="cargo_description" placeholder="Describe cargo..."/>
                            </page>
                            <page string="Lines">
                                <field name="line_ids">
                                    <tree editable="bottom">
                                        <field name="sequence" widget="handle"/>
                                        <field name="product_id"/>
                                        <field name="description"/>
                                        <field name="quantity"/>
                                        <field name="price_unit"/>
                                        <field name="subtotal"/>
                                    </tree>
                                </field>
                            </page>
                            <page string="Notes">
                                <field name="notes" placeholder="Notes..."/>
                            </page>
                        </notebook>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids"/>
                        <field name="activity_ids"/>
                        <field name="message_ids"/>
                    </div>
                </form>
            </field>
        </record>

        <record id="view_tms_waybill_tree" model="ir.ui.view">
            <field name="name">tms.waybill.tree</field>
            <field name="model">tms.waybill</field>
            <field name="arch" type="xml">
                <tree string="Waybills">
                    <field name="name"/>
                    <field name="date"/>
                    <field name="partner_id"/>
                    <field name="driver_id"/>
                    <field name="origin"/>
                    <field name="destination"/>
                    <field name="amount_total" widget="monetary"/>
                    <field name="state" widget="badge"/>
                </tree>
            </field>
        </record>

        <record id="action_tms_waybill" model="ir.actions.act_window">
            <field name="name">Waybills</field>
            <field name="res_model">tms.waybill</field>
            <field name="view_mode">tree,form</field>
        </record>

    </data>
</odoo>
